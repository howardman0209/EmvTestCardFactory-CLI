name: Publish CLI release notes

on:
  push:
    branches: [main]
    paths: ['releases/cli-*.md']

permissions:
  contents: write

concurrency:
  group: cli-public-release
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false
      - name: Verify and publish existing drafts
        env:
          GH_TOKEN: ${{ github.token }}
        run: python3 scripts/publish_cli_release.py
